# UWSM environment — toolkit and XDG vars
# Applied before Hyprland starts. Keeps hyprland.lua clean of hl.env() calls.
# See: https://wiki.hypr.land/Configuring/Advanced-and-Cool/Environment-variables/

export XCURSOR_SIZE=24
export XCURSOR_THEME=Adwaita

# GTK/Qt Wayland backends
export GDK_BACKEND=wayland,x11,*
export QT_QPA_PLATFORM=wayland;xcb
export QT_AUTO_SCREEN_SCALE_FACTOR=1
export QT_WAYLAND_DISABLE_WINDOWDECORATION=1
export SDL_VIDEODRIVER=wayland

# XDG session identity — required for portals (screenshare, file picker, etc.)
export XDG_CURRENT_DESKTOP=Hyprland
export XDG_SESSION_TYPE=wayland
export XDG_SESSION_DESKTOP=Hyprland
