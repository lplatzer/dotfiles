# Planning artifacts — not deployed to home directory
Plans/

# PowerShell install script — Windows only
{{ if ne .chezmoi.os "windows" -}}
install-tools.ps1
{{ end -}}

# macOS-only configs — skip on Linux
{{ if ne .chezmoi.os "darwin" }}
Brewfile
.config/linearmouse
.aerospace.toml
{{ end }}

# Linux-only configs — skip on macOS/Windows (Hyprland is a Wayland compositor)
{{ if ne .chezmoi.os "linux" }}
.config/hypr
{{ end }}
